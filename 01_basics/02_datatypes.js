        "use strict"; //treat all JS code as a newer version 
        
       // alert(3 + 3) //results an error since we are using nodejs, not a browser

      /*datatypes*/ 
       let name ="Jack"
       let age = 18
       let isLoggedIn = false
       let state;

        // number => 2 to the power 53
        // bigint
        // string => " "
        // boolean => true/false
        // null => standalone value
        // undefined => example=let state;
        // symbol => unique

        //object

        console.log(typeof null)  //the type is object(output)
        console.log(typeof undefined) //the type is undefined(output)
